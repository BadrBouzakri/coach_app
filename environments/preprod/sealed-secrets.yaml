# Placeholder for Sealed Secrets
# To create sealed secrets, run:
# ./scripts/create-sealed-secret.sh preprod

# Example structure (will be replaced by actual sealed secrets):
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: mongodb-secret
#   namespace: coach-app
# spec:
#   encryptedData:
#     username: <encrypted-value>
#     password: <encrypted-value>

# For now, using regular secrets (REPLACE IN PRODUCTION!)
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: coach-app
type: Opaque
stringData:
  username: admin
  password: CHANGE_ME_PREPROD_MONGODB_PASSWORD
---
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: coach-app
type: Opaque
stringData:
  jwt-secret: CHANGE_ME_PREPROD_JWT_SECRET
  mongodb-uri: mongodb://admin:CHANGE_ME_PREPROD_MONGODB_PASSWORD@mongodb-service:27017/coach_app?authSource=admin
