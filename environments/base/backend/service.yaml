apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: coach-app
  labels:
    app: backend
    component: api
spec:
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
