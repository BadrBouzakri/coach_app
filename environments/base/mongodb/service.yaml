apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: coach-app
  labels:
    app: mongodb
    component: database
spec:
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  type: ClusterIP
