apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: coach-app
  labels:
    app: frontend
    component: web
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
